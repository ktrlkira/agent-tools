[project]
name = "godot-mcp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mcps/godot"]
